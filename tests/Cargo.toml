[package]
name = "protocol-tests"
version = "1.0.1" # do not change this, no point, it never gets published.
authors = ["Dylan McKay <me@dylanmckay.io>"]

publish = false

[dependencies]
protocol = { path = "../protocol" }
protocol-derive = { path = "../protocol-derive" }

